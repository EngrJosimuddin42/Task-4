This file is used to configure the Gradle build system.

⭐ ফিক্স: AndroidX compatibility flags যোগ করা হলো, যা ডিপেন্ডেন্সি সমস্যা সমাধান করতে পারে এবং build warning দূর করবে। ⭐

android.useAndroidX=true
android.enableJetifier=true

The custom 'org.gradle.java.home' path has been removed to rely on system defaults,

as the forward-slash path fixed the parsing issue, but the Kotlin error remains.

This property increases JVM memory and prevents Out-of-memory errors in the build process.

org.gradle.jvmargs=-Xmx2048M --add-opens=java.base/java.io=ALL-UNNAMED